# GTOOL-002 shell-result truth published

handoff_id: `ghost-gtool-002-shell-result-truth-published-20260827`
supersedes: `ghost-gtool-002-shell-result-truth-qualified-20260827`
handoff_status: `superseded`
history: `HISTORY#056`
created_at: `2026-08-27T18:07:51-05:00`

## Protected-main result

PR #24 merged exact source
`88a4cace14352ed69d858e9d96f913108ed5751a` as implementation merge
`783964699d93c12725c7f91bdccf2bb1ecfcd008`. Corrected exact-source run
`33125003964` and exact merge-head run `33125085772` passed all six protected
jobs: repository hygiene, brand assets, Python 3.11, Python 3.13, package smoke,
and Stage 1 evaluations.

Protected source now carries `ghost.command_result/v1` from completed shell
execution through LangChain transport and the Ghost lifecycle. A nonzero exit
reaches SEAM action evidence as failed with its real exit code and cannot return
a passed verification ID or support an accepted outcome. Missing, malformed,
stale, duplicated, mismatched, refused, timed-out, or blank-ID evidence fails
closed.

The registered artifact report is
`docs/audits/2026-08-27-gtool-002-shell-result-truth.md`. The primary dirty
avatar checkout remains untouched.

This is merged source, not a package publication, release, deployment, paid or
live-provider validation, live SEAM service qualification, or completion of the
remaining remediation ledger.

## Resume order

1. Start GTOOL-003 in a new focused worktree from current protected main.
   Terminate and reap the complete process group on timeout, then prove no
   descendant can perform a delayed side effect after the tool returns.
2. Close GTOOL-004 runtime output bounds so stdout/stderr cannot grow
   proportionally in memory before truncation.
3. Keep `GHOST_ENABLE_SHELL` off for normal operation until GTOOL-003,
   GTOOL-004, GST-001, and GST-002 close.
4. Then close GTOOL-005 request redaction, resource/checkpoint/transport/CLI
   boundaries, and GPKG-001 before release-candidate work.
5. Build provider-free Q3 G1/G2 mechanics after the immediate lane and stop
   before paid execution for explicit operator approval.
