# Company licensing foundation placed

handoff_id: `ghost-company-licensing-foundation-20260825`
supersedes: `ghost-continuity-template-integrated-20260825`
handoff_status: `superseded`
history: `HISTORY#027`
created_at: `2026-08-25T15:30:00-05:00`

## Completed local boundary

- Ghost now carries the exact PolyForm Shield 1.0.0 text, required notice,
  package metadata, source-available language, and credential-free consistency
  tests.
- The company blueprint places Apache-2.0 on thin integration surfaces,
  PolyForm Shield on distributed runnable products, and permanent proprietary
  controls on undistributed runtime/model/control-plane assets.
- The adjacent local SEAM SDK, Canticle Core, and SEAM Node candidates carry
  Shield in their intended product repositories; the thin client remains
  Apache-2.0.
- The noncanonical SEAM Runtime scaffold records the proprietary target without
  claiming to relicense the canonical private runtime.
- Interim contribution and trademark policies protect the pre-company
  ownership boundary.
- Canticle Core and SEAM-U remain explicitly planned; no implementation or
  model checkpoint is claimed.

## Verification

- official Shield text compared byte-for-byte across Ghost, SEAM SDK, Canticle
  Core, and SEAM Node;
- Ghost provider-free suite: 190 passed with eight live tests deselected;
- SEAM SDK: nine tests passed and Ruff passed with the canonical runtime on
  `PYTHONPATH`;
- Apache thin client: 58 tests passed and Ruff passed;
- Ghost and SDK package builds succeeded, exposed the Shield `LicenseRef`, and
  included `LICENSE` and `NOTICE`;
- scoped Ghost Ruff and `git diff --check` passed; and
- full Ghost Ruff retained five known avatar/image-tool findings unrelated to
  this licensing change.

## Locality and legal limits

- No Ghost or SEAM SDK license candidate is committed, pushed, merged,
  released, or deployed.
- Canticle Core, SEAM Node, SEAM Runtime, and SEAM Client paths inspected here
  are local non-Git scaffolds/packages in this workspace.
- No Canticle legal entity or founder-to-company IP assignment is recorded.
- This is an engineering distribution architecture, not counsel approval or a
  conclusion about enforceability in a particular jurisdiction.
- The canonical private SEAM repository remains untouched and needs its own
  governed reconciliation.

## Next work

1. Give the product matrix, notices, IP inventory, and planned revenue surfaces
   to qualified startup/IP counsel before commercial launch.
2. Form the entity and execute the founder IP assignment before changing the
   licensor name.
3. Review and safely publish the Ghost continuity/license candidate without
   exposing the private runner.
4. Reconcile and, if approved, update the canonical private SEAM runtime under
   its repository-specific continuity protocol.
5. Build the versioned public API/client boundary before calling Ghost publicly
   installable.

## Preserve

- Do not reset or delete the local avatar source, assets, tests, or art tools.
- Keep the Apache thin client free of private runtime/MIRL implementation.
- Do not publish SEAM-U implementation claims, weights, or data claims before
  those artifacts exist and pass provenance review.
- Do not replace the founder's name with a future company name before written
  assignment.
